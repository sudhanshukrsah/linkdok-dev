# ==============================================
# Environment Variables Example
# ==============================================
# This file shows what environment variables are needed.
# Copy this to .env for local development, but NEVER commit
# your actual .env file with real API keys!
# 
# For production on Vercel:
# Set these in Project Settings > Environment Variables
# ==============================================

# ── NVIDIA NIM API Key (PRIMARY — all AI models) ──────────────
# Get your key from: https://build.nvidia.com → API Keys
# On Vercel, set as: NVIDIA_API_KEY
NVIDIA_API_KEY=your_nvidia_nim_api_key_here

# ── OpenRouter API Key (FALLBACK — if NVIDIA fails) ───────────
# Get your key from: https://openrouter.ai/keys
# On Vercel, set as: OPENROUTER_API_KEY
OPENROUTER_API_KEY=your_openrouter_api_key_here

# ── Diffbot Token (Content Extraction) ────────────────────────
# Get your token from: https://www.diffbot.com/
# On Vercel, set as: DIFFBOT_TOKEN
# Optional — if not set, Jina AI & AllOrigins fallbacks are used
DIFFBOT_TOKEN=your_diffbot_token_here

# ── App URL (for CORS & OpenRouter referer tracking) ──────────
# Your production URL, e.g. https://linkdok.vercel.app
# On Vercel, set as: APP_URL
APP_URL=https://your-app.vercel.app

